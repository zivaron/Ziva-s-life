# הנחיות לפרויקט Ziva-s-life

תיקיית־על לפרויקטים אישיים של זיוה רון. כל פרויקט בתיקייה משלו.

## כללי עבודה (חלים על כל הפרויקטים כאן)

- **הכול בעברית.** מסמכים, הערות, כותרות, שמות קבצים ותשובות בצ׳אט — עברית בלבד.
  אין להשאיר מונחים לועזיים או מצייני מקום באותיות לטיניות בתוך מסמכים מוגמרים
  (נמצאו ותוקנו כאלה בעבר: `X`, `comma splice`, `Sunny`). לפני מסירת מסמך — לסרוק
  אותו לתווים לטיניים.
- **זיוה עובדת ב-Word.** התוצרים צריכים להיות `.docx`, לא Markdown ולא HTML.
- **יישור לימין תמיד.** זו בקשה מפורשת של זיוה לכל מסמך עתידי: עברית מיושרת
  לימין, ומירכוז רק היכן שהוא באמת נדרש — שערים, כותרות פרקים, שירה, פזמונים
  וכתובות על עלי הזהב. גוף טקסט לעולם לא ממורכז.
  ליישום: **לא לציין `alignment` כלל** לפסקאות עברית (ראה המלכודת בהמשך —
  `alignment: RIGHT` דוחף את הטקסט דווקא שמאלה), ו-`CENTER` רק במקומות שלמעלה.
- **`analysis/03_נוסח_מוצע_מלא.docx` הוא תבנית העיצוב המאושרת.** זיוה אישרה
  אותו במפורש כתקין וכרצוי. כל מסמך חדש — לבנות באותו אופן, דרך
  `tools/lib_docx.js` + `tools/fix_rtl.py`, ולא להמציא עיצוב מחדש.
- הקבצים נשמרים בגיט. הקונטיינר זמני — כל דבר שלא נדחף, אבד.

---

## פרויקט: "סאני וחבורת חמשת הכוחות" (ספר ילדים)

ספר ילדים מאת זיוה רון. כתב היד המקורי: 2,686 מילים, 7 פרקים. נכתב עבור שני
ילדים אמיתיים: **סאני** היא האחיינית, **תובלי** הוא הבן של זיוה.

**הגרסה הפעילה כרגע היא לא כתב היד המקורי ולא מסמך 03** — היא הגרסה שזיוה
כתבה בעצמה (`manuscript/סאני_גרסה_אחרונה_של_זיוה.docx`, 3,562 מילים). כל עבודה
חדשה יוצאת ממנה. ראי "גרסאות הספר" בהמשך למפת המעבר המלאה.

### מבנה התיקיות

| נתיב | תוכן |
|---|---|
| `manuscript/סאני_וחבורת_חמשת_הכוחות_מקור.docx` | כתב היד המקורי כפי שהתקבל. **לא לשנות.** |
| `manuscript/סאני_טקסט_מקורי.txt` | חילוץ טקסט נקי של המקור |
| `manuscript/סאני_גרסה_אחרונה_של_זיוה.docx` | **הגרסה הפעילה.** כתובה בידי זיוה, 3,562 מילים |
| `manuscript/סאני_גרסה_אחרונה_טקסט.txt` | חילוץ טקסט נקי של הגרסה הפעילה |
| `analysis/01_ניתוח_ספרותי_מקיף.docx` | ניתוח מ־20 בחינות + חוות דעת + תוכנית פעולה + מפת איור (על המקור) |
| `analysis/02_הטקסט_עם_הערות_עריכה.docx` | המקור עם 64 הערות משובצות + 21 תיקונים + קטעים משוכתבים |
| `analysis/03_נוסח_מוצע_מלא.docx` | הצעת שכתוב מלאה שהוגשה לזיוה. **שימשה בסיס — אינה הגרסה הפעילה** |
| `analysis/04_הערות_לגרסה_האחרונה.docx` | הערות על הגרסה הפעילה: 12 תיקוני חובה + 11 הערות תוכן |
| `analysis/05_דר_סוס_מדריך_הפעלה.docx` | כרטיס זהות + הוראות הפעלה לסוכן "ד״ר סוס" (ראי בהמשך) |
| `web/הספר.html` | הגרסה הפעילה + ההערות, כדף קריאה. פורסם כ-Artifact לגישה מכל מקום |
| `tools/lib_docx.js` | עוטף משותף לבניית `.docx` בעברית מימין לשמאל |
| `tools/fix_rtl.py` | משלים תיקוני XML שהעוטף לא מייצר נכון (ראי "מלכודות") |
| `tools/build_analysis.js`, `build_annotated.js`, `build_revised.js`, `build_notes_v2.js`, `build_dr_suss.js` | מייצרים את חמשת מסמכי ה-`analysis/` |
| `tools/build_web.py` | בונה את `web/הספר.html` ישירות מ-`manuscript/סאני_גרסה_אחרונה_של_זיוה.docx` |

בנייה מחדש של מסמך בודד: `node tools/build_<שם>.js && python3 tools/fix_rtl.py`
בנייה מחדש של הכול: להריץ את כל סקריפטי ה-`build_*.js` ואז `fix_rtl.py` פעם אחת בסוף
(הוא סורק את כל `analysis/*.docx`). לדף האינטרנט: `python3 tools/build_web.py`.

### מה יש בהצעת השכתוב (מסמך 03)

זו ההצעה שהוגשה לזיוה, לא הגרסה הפעילה. זיוה כתבה מאז גרסה משלה שמאמצת חלק
מזה ומשנה חלק אחר — ראי "גרסאות הספר" למטה למצב המדויק. השינויים העלילתיים
שהוצעו כאן, לעיון:

- **הכישלון:** בפרק 5 תמרה מחליקה ליד המים, סאני מצילה אותה, ומהכיס נופל
  **עלה החברות** (הראשון שנמצא) ונסחף. תובלי, שזה עלהו, אומר "תמרה גם שלי".
- **ההשבה:** בפרק 7 העלה נמצא תקוע בשורשי העץ העתיק — הנחל זרם לשם כל הזמן
  (נשתל באינטרלוד בסוף פרק 5). תובלי מבקש שסאני תניח אותו.
- **שעון מתקתק:** אינטרלוד קצר בסוף כל פרק על העץ שממשיך להיחלש.
- **פרטים אישיים:** תמרה — תמיד אוכל בכיס; אופיר — כיסים מלאים אבנים;
  אימרי — סופר בקול לפני דבר מפחיד; יונתן — מזמזם; תובלי — ממציא לכולם שמות.
- **הומור:** הוויכוח על "תמיד ימינה" (2), הבטן של תמרה (3), הספירה והארנב (4),
  הגשר שהיה שם כל הזמן (5), הכריך המעוך שמתחלק לשישה (6).
- חמשת הפתגמים כתובים על גב עלי הזהב, לא בפי הילדים.
- מילון "מילים חדשות שפגשנו ביער" בסוף + נספח טבלאי של השינויים לפי פרק.

### הדמויות

| דמות | כוח | הערות |
|---|---|---|
| סאני | השמש (העלה השישי) | הגיבורה. האחיינית. כוחה: לגלות את הכוחות של האחרים |
| תובלי | חברות | הבן של זיוה |
| יונתן | הקשבה | |
| אימרי | אומץ | **הכתיב "אימרי" — לא "אמרי".** במקור יש חוסר עקביות |
| אופיר | סקרנות | דמות נקבה |
| תמרה | תקווה | דמות נקבה |

סה"כ **שישה** ילדים. שם החבורה הוא "חמשת הכוחות" — הפער הזה מכוון ונפתר בתוך
העלילה עצמה (ראי "מה זיוה כבר אימצה" בהמשך).

### מסקנות הניתוח — לא לגזור מחדש

**שני הרעיונות החזקים בכתב היד:**
1. העלה השישי — סאני מקבלת שמש, כלומר תפקיד ולא כוח. תובנה בוגרת על מנהיגות.
2. שני העצים — עץ הגינה ועץ היער מתוארים כמעט מילה במילה זהים; אחד פורח ואחד קמל,
   וההבדל היחיד הוא שאת האחד מבקרים.

**שלוש הבעיות המרכזיות:**
1. שום דבר אף פעם לא קשה — אין יריב, אין מחיר, אף אחד לא נכשל.
2. סאני פסיבית — במשך חמישה פרקים היא רק נושאת את העלים בכיס.
3. הסבר כפול — המסר נאמר במפורש פעמיים בכל פרק (מספר + דמות).
   ובנוסף: אין בספר אף רגע מצחיק.

**סאבטקסט:** הספר הוא, מתחת לפני השטח, על זקנה ונטישה. "השנים חלפו, הילדים גדלו
ועזבו" ו"כבר לא האמין שמישהו יבוא לבקר אותו" — זה תיאור של סבא, לא של עץ.

### גרסאות הספר — מסלול העבודה המלא

1. כתב היד המקורי (2,686 מילים) →
2. ניתוח (מסמך 01) והערות (מסמך 02) על המקור →
3. הצעת שכתוב מלאה (מסמך 03, 3,198 מילים) — כדוגמה, לא לפרסום →
4. **זיוה כתבה גרסה משלה על בסיס מסמך 03** (`manuscript/סאני_גרסה_אחרונה_של_זיוה.docx`,
   3,562 מילים) — **זו הגרסה הפעילה** →
5. הערות על הגרסה הפעילה (מסמך 04) — 12 תיקוני חובה + 11 הערות תוכן →
6. דף אינטרנט (`web/הספר.html`) שמציג את הגרסה הפעילה + ההערות יחד.

### מה זיוה כבר אימצה מהגרסה הפעילה — לא לגזור מחדש

- שישה ילדים, שם החבורה "חמשת הכוחות" נשאר. **הפתרון בפועל שונה מההצעה
  המקורית:** זיוה הוסיפה סצנת מתן־שם בפרק 1 ופתרון בפרק 7 (אימרי/סאני מסבירים
  את הפער) — אבל **בלי** משפט הגילוי "איך קוראים לך? שמש" של אופיר בסוף.
- הכישלון האמיתי אומץ במלואו: עלה החברות נופל לנחל בפרק 5 וחוזר בכוחות עצמו
  בפרק 7, כולל המשפט "תמרה גם שלי" (בגרסתה: "...חברה שלי").
- שעון מתקתק, פרטים אישיים לכל ילד, פתגמים על גב העלים, מילון בסוף — כולם נשארו.
- שם פרק 7 הוכרע: "האור חזר אל היער".

### מה עדיין פתוח — לפי סדר עדיפות (מפורט במסמך 04)

- [ ] **סצנת גילוי השם בסוף** — זיוה הזיזה את הרמז ("שמש") לעמוד הראשון, באנגלית,
      ומחקה את סצנת הגילוי בסוף. ההמלצה העומדת: רמז עברי עדין בעמוד 1 בלבד,
      והחזרת סצנת הגילוי המלאה לסוף.
- [ ] **משפט הפעילות של סאני** ("לא מצאתי אף עלה, רק ידעתי בכל פעם על מי
      להסתכל") — נמחק מהגרסה הפעילה. בלעדיו חמשת רגעי הזיהוי שפזורים
      בפרקים לא מקבלים תשלום בסוף.
- [ ] **חרוז השיר** — זיוה שינתה את השורה האחרונה ל"יתגשם", שלא מתחרז עם
      "לוותר". יש נוסח חלופי שמתחרז ושומר על "יתגשם" אם היא חשובה לה.
- [ ] שני רגעי הומור שהוחלשו לניסוח מסביר (הוויכוח בפרק 2, "פחדת/פחדתי" בפרק 4).
- [ ] המסר הסוגר ("בכל אחד טמונים כל חמשת הכוחות") מבטל את חלוקת הכוח־לכל־ילד
      שהספר בנוי עליה — יש הצעת ניסוח חלופי במסמך 04.
- [ ] רשימת תיקוני לשון ופיסוק קטנים (טבלה מלאה במסמך 04).
- [ ] גיל היעד (ההמלצה: 5–8, ספר הקראה שילד גדול יקרא גם לבד) — עדיין לא הוכרע.

**הערה חשובה:** זיוה עוד לא ביקשה לערוך את `manuscript/סאני_גרסה_אחרונה_של_זיוה.docx`
בפועל. כל מה שלמעלה הן המלצות. אין לשנות אף קובץ ב-`manuscript/` בלי בקשה מפורשת.

### ד״ר סוס — הסוכן הקבוע (analysis/05)

כרטיס זהות והוראות הפעלה של "ד״ר סוס" — פרסונה שזיוה יכולה לטעון לפרויקט
בקלוד (Project) כדי לקבל את אותה שיטת עבודה גם מחוץ לקלוד קוד. מכיל: שיטת
עשרים הבחינות, טבלת הדמויות, מצב "מה הוחלט / מה פתוח" (התמציתי, לא המלא),
וכללי הברזל של הפרויקט. **כל פעם שמשהו מ"מה עדיין פתוח" למעלה מוכרע, או
שמגיעה גרסה חדשה — לעדכן גם את `analysis/05` וגם את הקטע הזה כאן, באותה
נשימה, כדי ששני מקורות הזיכרון לא יסתרו זה את זה.** בנייה מחדש:
`node tools/build_dr_suss.js && python3 tools/fix_rtl.py`

---

## מגבלות סביבה שנתקלנו בהן

- **LibreOffice שבור כאן.** `soffice` מחזיר "source file could not be loaded" גם על
  קבצי Word תקינים. אי אפשר להמיר `.docx` ל-PDF ואי אפשר לבדוק עימוד ויזואלית.
  במקום זה: לאמת עם `validate.py` ולחלץ טקסט חזרה עם `zipfile` + `ElementTree`.
- `pandoc`, `pdftoppm`, `pymupdf`, `lxml`, `defusedxml` לא מותקנים מראש
  (חלקם הותקנו בסשן; עלולים להיעדר בסשן חדש).

## מלכודות ב-docx-js שנפלנו בהן

- **`\n` בתוך `TextRun` לא יוצר שורה חדשה** — Word מכווץ אותו לרווח. `lib_docx.js`
  מפצל על `\n` ומוסיף `break: 1`. אם מוסיפים שירה או קטע רב־שורות — לוודא שזה עובר
  דרך `P()` ולא ישירות ל-`TextRun`.
- **המלכודת הגדולה ביותר — `left`/`right` הם לוגיים, לא פיזיים.** ב-OOXML הערכים
  האלה ב-`w:jc`, ב-`w:ind` וב-`w:pBdr` הם כינויים מיושנים ל-`start`/`end`.
  בפסקה עם `<w:bidi/>` הצד המתחיל הוא **ימין**, ולכן:

  | מה שכותבים | מה שקורה בעברית |
  |---|---|
  | `alignment: RIGHT` | `jc="right"` = הקצה המסיים → **הטקסט נדחף שמאלה** ✗ |
  | בלי `alignment` | ברירת מחדל = הקצה המתחיל → **מיושר לימין** ✓ |
  | `indent: { right: N }` | הזחה מהקצה השמאלי ✗ |
  | `indent: { left: N }` | הזחה מהקצה הימני ✓ |
  | `border: { right }` | פס בצד שמאל ✗ |
  | `border: { left }` | פס בצד ימין ✓ |

  זה בדיוק מה שקרה כאן: 452 פסקאות עם `alignment: RIGHT` נראו מיושרות שמאלה.
  **אין לציין יישור לפסקאות עברית רגילות.** `CENTER` נייטרלי ולכן בטוח.
  לאימות: לספור `w:val="right"` במסמך — חייב להיות 0.
  אמת מידה: קובץ ה-Word המקורי של זיוה משתמש ב-`jc="both"` ואף פעם לא ב-`right`.

- **לעברית תקינה נדרשות שש שכבות, לא שתיים.** לסמן פסקאות וריצות זה לא מספיק:

  | שכבה | איך | מה קורה בלעדיה |
  |---|---|---|
  | פסקה | `bidirectional: true` | הטקסט מיושר שמאלה |
  | ריצה | `rightToLeft: true` | פיסוק קופץ לקצה הלא נכון |
  | טבלה | `visuallyRightToLeft: true` | סדר העמודות מתהפך |
  | ברירת מחדל | `styles.default.document.run.rightToLeft` | ריצה שלא עברה בעוטפים נשארת לטינית |
  | רמת רשימה | `style.run.rightToLeft` + `style.paragraph.bidirectional` + `alignment: RIGHT` | התבליט נוחת משמאל, ו־"1." מוצג כ־".1" |
  | **מקטע** | **אין ב-docx-js — ראה `tools/fix_rtl.py`** | **המסמך כולו נשאר משמאל לימין** |

- `docx-js` לא חושפת כיווניות ברמת המקטע. `tools/fix_rtl.py` מזריק ל-XML
  `<w:bidi/>` ו-`<w:rtlGutter/>` בתוך `<w:sectPr>`. **חובה להריץ אותו אחרי כל בנייה:**
  `node tools/build_*.js && python3 tools/fix_rtl.py`
  לפי סכמת ECMA-376 שני האלמנטים חייבים לבוא אחרי `<w:pgNumType>` ולפני
  `<w:docGrid>` — סדר האלמנטים נאכף, והזרקה במקום אחר פוסלת את הקובץ.
- `szCs`/`bCs`/`iCs`/`rFonts w:cs` (מאפייני כתב מורכב, שהם אלה שקובעים איך Word
  מציג עברית) — docx-js מייצרת אותם לבד. לא צריך לטפל בזה.
- בדיקה מהירה: לספור פסקאות בלי `<w:bidi>` וריצות בלי `<w:rtl>` — שתיהן צריכות
  להיות 0. ריצות של `<w:br>` בלבד (מעברי עמוד) תקינות בלי `rtl`.
- **גבול בארבעה צדדים לפסקה (`P(..., {box: color})`) פוסל את הקובץ.** docx-js
  פולטת תמיד בסדר `top, bottom, left, right` — קבוע בספרייה, לא משתנה לפי סדר
  המפתחות באובייקט ב-JS. הסכמה (`CT_PBdr`) דורשת `top, left, bottom, right`.
  `tools/fix_rtl.py` מסדר את זה מחדש אוטומטית בכל קובץ (`reorder_borders`) —
  לכן `box` בטוח לשימוש כל עוד מריצים `fix_rtl.py` אחרי הבנייה, כמו תמיד.
  גבול בצד אחד בלבד (`leftBar`, או ה-`bottom` של `RULE()`) לא נפגע מזה מלכתחילה.

## גיט

ענף העבודה: `claude/ziva-book-analysis-r9m8hz`. לדחוף תמיד עם
`git push -u origin <branch>`. לא נפתח Pull Request — זיוה לא ביקשה.
